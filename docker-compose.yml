services:
  
  themoviedb_service:
    build: .
    container_name: themoviedb_service
    restart: always
    env_file:
      - .env
    ports:
      - "${REST_PORT}:${REST_PORT}"

volumes:
  postgres_data: